<?php

//This term is: Passing arguments to a function
//The 30,30 in the "add" parameters below are the arguments (or values) you are passing into the function.
function add($num1 , $num2){
return $num1 * $num2;
}
echo "<h2> The answer to 30 times 30 is " . add(30,30 ) . "<h2>";
?>
  

init: // Initialization code (or boot code) takes the processor from the reset state to a state where the operating system can run

add_action
//The add_action function is arguably the most used function in WordPress. Simply put, it allows you to run a function when a particular hook occurs

